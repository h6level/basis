import React from 'react';

interface IProps {}
interface IState {}

const Home = (props: IProps, state: IState) => {
  return (
    <section>
      <h1>****** Home Page ******</h1>
    </section>
  );
};

export default Home;
